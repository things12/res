# Resume Analyzer (single-input)

## Backend
- Endpoint: POST /analyze_resume (file form-data)
- Run:
  ```bash
  cd backend
  pip install -r requirements.txt
  uvicorn main:app --reload --port 8000
  ```

## Frontend
- Run:
  ```bash
  cd frontend
  npm install
  npm run dev
  ```
